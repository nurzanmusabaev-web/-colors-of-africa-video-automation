# 86 animated clips, 7s each, 1920×1080

Higgsfield `kling3_0_turbo`, 1080p, 7s, 14 credits each = 1204 credits total.
Source: the final 86 frames in `../frames.tsv` (each clip starts from its
corresponding still).

Download: `download-clips.bat` (Windows) → `secretary-clips\0_00.mp4` .. `9_55.mp4`.

Motion direction per shot is in `../motion.json`, keyed by timecode.
